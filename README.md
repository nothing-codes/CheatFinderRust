# Cheat Finder Rust

### Поддерживаемые типы читов:

- **Rust Pirate (ALKAD)** — читы для пиратской версии игры
- **Rust Official (STEAM)** — читы для официальной версии игры из Steam

## ✨ Возможности

- ✅ Сканирование всех дисков компьютера
- ✅ Поиск читов для Rust Pirate (ALKAD)
- ✅ Поиск читов для Rust Official (STEAM)
- ✅ Поиск общих читов
- ✅ Графический интерфейс с таблицей результатов
- ✅ Экспорт результатов в текстовый файл
- ✅ Лог сканирования в реальном времени
- ✅ Прогресс-бар сканирования
- ✅ Возможность остановки сканирования
- ✅ Поиск только по точному названию из списка

## 🔨 Сборка проекта

```bash
dotnet build
```

## 📦 Публикация в .exe

```bash
dotnet publish -c Release
```

Готовый .exe файл будет находиться в папке:
`bin/Release/net8.0-windows/win-x64/publish/CheatFinderRust.exe`

## 🚀 Использование

### Запуск программы:

1. Запустите `CheatFinderRust.exe`
2. В главном окне вы увидите:
   - Кнопки управления: "Начать сканирование", "Остановить", "Экспорт результатов"
   - Таблицу результатов (слева)
   - Лог сканирования (справа)
   - Прогресс-бар внизу

### Процесс сканирования:

1. Нажмите кнопку **"Начать сканирование"**
2. Программа начнет сканировать все диски компьютера
3. Найденные читы будут отображаться в таблице в реальном времени
4. В логе справа вы увидите подробную информацию о процессе сканирования
5. При необходимости вы можете остановить сканирование кнопкой **"Остановить"**

### Работа с результатами:

- **Просмотр результатов** — все найденные читы отображаются в таблице с указанием названия, пути и времени обнаружения
- **Экспорт результатов** — нажмите кнопку **"Экспорт результатов"** для сохранения всех найденных читов в текстовый файл
- **Лог сканирования** — в правой панели отображается подробный лог всех операций сканирования

## ⚙️ Настройка списка читов

Список читов для поиска хранится в файле `Data/cheats.json`. Этот файл автоматически копируется в папку с исполняемым файлом при сборке проекта.

### Редактирование списка:

1. Откройте файл `Data/cheats.json` в любом текстовом редакторе
2. Добавьте или удалите названия читов в массиве `cheats`
3. Сохраните файл
4. Пересоберите проект или скопируйте обновленный файл в папку с программой

### Формат файла:

```json
{
  "cheats": [
    "ALKAD",
    "alkad",
    "RustPirate",
    "rustpirate",
    "RustOfficial",
    "rustofficial",
    "cheat.dll",
    "hack.dll"
  ]
}
```

### Правила поиска:

- **Точное совпадение** — программа ищет только файлы и папки, названия которых точно совпадают с названиями из списка (без учета регистра)
- **Для файлов** — проверяется как полное имя файла (с расширением), так и имя без расширения
- **Для папок** — проверяется только точное название папки

**Пример:** Если в списке есть `"rust.dll"`, программа найдет файл `rust.dll`, но не найдет `my_rust.dll` или `rust_backup.dll`.

## 🔧 Технические детали

### Технологии:

- **.NET 8.0** — платформа разработки
- **Windows Forms** — графический интерфейс
- **Newtonsoft.Json** — работа с JSON-файлами
- **C#** — язык программирования

### Архитектура:

- **Models** — модели данных (`CheatInfo`, `CheatList`)
- **Services** — бизнес-логика (`CheatScanner`)
- **Forms** — пользовательский интерфейс (`MainForm`)
- **Data** — конфигурационные файлы (`cheats.json`)

### Особенности реализации:

- Асинхронное сканирование с использованием `async/await`
- Отмена операции через `CancellationToken`
- Прогресс сканирования через `IProgress<T>`
- Обработка ошибок доступа к файлам и папкам
- Пропуск системных папок для ускорения сканирования

## 📋 Требования

- **Операционная система:** Windows 10 или выше
- **.NET Runtime:** .NET 8.0 Desktop Runtime (включен в SelfContained сборку)
- **Права доступа:** Для сканирования всех дисков могут потребоваться права администратора

## 📝 Лицензия

Этот проект создан в образовательных целях и распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробной информации.

## ⚠️ Замечания

- Программа сканирует все диски, что может занять значительное время на больших дисках
- Некоторые папки могут быть недоступны из-за ограничений прав доступа
- Программа пропускает системные папки (Windows, System Volume Information и т.д.) для ускорения работы
